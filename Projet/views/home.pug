html
    header
        title Home
        h1 Bienvenue
    body
        for question in questions
            article
                a(href="/detail/" + question.id) #{question.title}
                p #{question.user.firstname}

        a(href="/login") Se connecter
        a(href="/signup") S'inscrire
        if user
            a(href="/addquestion") Poser une question
        else
            p Veuillez vous connecter pour poster une question.